# refugee-identity-system (workspace project)

This folder contains the AlgoKit-generated workspace project. The application itself lives under:

- `Nexathon/` (React frontend + FastAPI backend + Algorand contract artifacts)

For the full documentation (screenshots, quickstart, and end-to-end flows), see the workspace root README:

- `../../README.md`


